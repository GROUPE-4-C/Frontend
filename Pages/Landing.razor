@page "/"
@layout MainLayout

<link href="css/landing.css" rel="stylesheet" />


    <!-- Header -->
    <header>
        <nav class="container">
            <a href="#" class="logo">AlumniConnect</a>
            <ul class="nav-links">
                <li><a href="#features">Fonctionnalités</a></li>
                <li><a href="#alumni">Alumni</a></li>
                <li><a href="#about">À propos</a></li>
                <li><a href="#contact">Contact</a></li>
            </ul>
             <div class="nav-buttons">
                <a href="/login" class="btn btn-secondary">Connexion</a>
                <a href="/register" class="btn btn-primary">Inscription</a>
            </div>

        </nav>
    </header>

    <!-- Hero Section -->
    <section class="hero">
        <div class="container">
            <div class="hero-content fade-in">
                <h1>Reconnectez-vous avec vos anciens</h1>
                <p>AlumniConnect est la plateforme qui unit les diplômés, favorise les opportunités et construit un réseau professionnel solide pour votre avenir.</p>
                <div class="hero-buttons">
                    <a href="/register" class="btn btn-primary btn-large">Rejoindre le réseau</a>
                    <a href="/dashboard" class="btn btn-secondary btn-large">Découvrir plus</a>
                </div>
            </div>
        </div>
    </section>

    <!-- Stats Section -->
    <section class="stats">
        <div class="container">
            <div class="stats-grid">
                <div class="stat-item fade-in">
                    <div class="stat-number">12,000+</div>
                    <div class="stat-label">Alumni connectés</div>
                </div>
                <div class="stat-item fade-in">
                    <div class="stat-number">50+</div>
                    <div class="stat-label">Établissements partenaires</div>
                </div>
                <div class="stat-item fade-in">
                    <div class="stat-number">95%</div>
                    <div class="stat-label">Taux de satisfaction</div>
                </div>
                <div class="stat-item fade-in">
                    <div class="stat-number">2,500+</div>
                    <div class="stat-label">Opportunités créées</div>
                </div>
            </div>
        </div>
    </section>

    <!-- Features Section -->
    <section class="features" id="features">
        <div class="container">
            <div class="section-title">
                <h2>Fonctionnalités principales</h2>
                <p>Découvrez tous les outils et services qui font d'AlumniConnect la plateforme de référence pour les diplômés</p>
            </div>
            <div class="features-grid">
                <div class="feature-card fade-in">
                    <div class="feature-icon">👥</div>
                    <h3>Réseau professionnel</h3>
                    <p>Connectez-vous avec des milliers d'anciens diplômés dans votre domaine et élargissez votre réseau professionnel.</p>
                </div>
                <div class="feature-card fade-in">
                    <div class="feature-icon">💼</div>
                    <h3>Opportunités d'emploi</h3>
                    <p>Accédez à des offres d'emploi exclusives et des opportunités de carrière partagées par votre communauté.</p>
                </div>
                <div class="feature-card fade-in">
                    <div class="feature-icon">🎓</div>
                    <h3>Mentorship</h3>
                    <p>Trouvez des mentors expérimentés ou devenez mentor pour accompagner la nouvelle génération.</p>
                </div>
                <div class="feature-card fade-in">
                    <div class="feature-icon">🌐</div>
                    <h3>Événements</h3>
                    <p>Participez à des événements exclusifs, des conférences et des rencontres organisées par et pour les alumni.</p>
                </div>
                <div class="feature-card fade-in">
                    <div class="feature-icon">📚</div>
                    <h3>Ressources</h3>
                    <p>Accédez à une bibliothèque de ressources, formations et outils pour votre développement professionnel.</p>
                </div>
                <div class="feature-card fade-in">
                    <div class="feature-icon">🏆</div>
                    <h3>Reconnaissance</h3>
                    <p>Mettez en valeur vos réalisations et célébrez les succès de votre communauté d'alumni.</p>
                </div>
            </div>
        </div>
    </section>

    <!-- Alumni Spotlight -->
    <section class="alumni-spotlight" id="alumni">
        <div class="container">
            <div class="section-title">
                <h2>Témoignages Alumni</h2>
                <p>Découvrez comment AlumniConnect a transformé la carrière de nos membres</p>
            </div>
            <div class="alumni-grid">
                <div class="alumni-card fade-in">
                    <div class="alumni-avatar">MR</div>
                    <div class="alumni-name">Marie Rousseau</div>
                    <div class="alumni-title">Directrice Marketing • TechCorp</div>
                    <div class="alumni-quote">"Grâce à AlumniConnect, j'ai trouvé mon poste actuel et construit un réseau incroyable. Une plateforme indispensable pour tout diplômé."</div>
                </div>
                <div class="alumni-card fade-in">
                    <div class="alumni-avatar">JD</div>
                    <div class="alumni-name">Jean Dupont</div>
                    <div class="alumni-title">Entrepreneur • StartupTech</div>
                    <div class="alumni-quote">"Les connexions que j'ai établies sur AlumniConnect m'ont aidé à lancer ma startup. La communauté est exceptionnelle."</div>
                </div>
                <div class="alumni-card fade-in">
                    <div class="alumni-avatar">SL</div>
                    <div class="alumni-name">Sophie Laurent</div>
                    <div class="alumni-title">Consultante • McKinsey & Co</div>
                    <div class="alumni-quote">"Le programme de mentorship m'a permis de progresser rapidement dans ma carrière. Je recommande vivement AlumniConnect."</div>
                </div>
            </div>
        </div>
    </section>

    <!-- CTA Section -->
    <section class="cta">
        <div class="container">
            <h2>Prêt à rejoindre la communauté ?</h2>
            <p>Rejoignez des milliers d'alumni qui ont déjà transformé leur carrière grâce à AlumniConnect</p>
            <div class="hero-buttons">
                <a href="/register" class="btn btn-primary btn-large">Créer mon compte</a>
                <a href="#about" class="btn btn-secondary btn-large">En savoir plus</a>
            </div>
        </div>
    </section>

    <!-- Footer -->
    <footer>
        <div class="container">
            <div class="footer-content">
                <div class="footer-section">
                    <h3>AlumniConnect</h3>
                    <p>La plateforme qui connecte les diplômés et crée des opportunités pour tous.</p>
                </div>
                <div class="footer-section">
                    <h3>Liens rapides</h3>
                    <ul>
                        <li><a href="#">Accueil</a></li>
                        <li><a href="#">Fonctionnalités</a></li>
                        <li><a href="#">À propos</a></li>
                        <li><a href="#">Contact</a></li>
                    </ul>
                </div>
                <div class="footer-section">
                    <h3>Support</h3>
                    <ul>
                        <li><a href="#">Centre d'aide</a></li>
                        <li><a href="#">FAQ</a></li>
                        <li><a href="#">Conditions d'utilisation</a></li>
                        <li><a href="#">Politique de confidentialité</a></li>
                    </ul>
                </div>
                <div class="footer-section">
                    <h3>Contact</h3>
                    <ul>
                        <li>📧 contact@alumniconnect.com</li>
                        <li>📞 +228 90 00 00 00</li>
                        <li>📍 Lomé, Togo</li>
                    </ul>
                </div>
            </div>
            <div class="footer-bottom">
                <p>&copy; 2024 AlumniConnect. Tous droits réservés.</p>
            </div>
        </div>
    </footer>

    <script>
        // Smooth scrolling for navigation links
        document.querySelectorAll('a[href^="#"]').forEach(anchor => {
            anchor.addEventListener('click', function (e) {
                e.preventDefault();
                const target = document.querySelector(this.getAttribute('href'));
                if (target) {
                    target.scrollIntoView({
                        behavior: 'smooth',
                        block: 'start'
                    });
                }
            });
        });

        // Animation on scroll
        const observerOptions = {
            threshold: 0.1,
            rootMargin: '0px 0px -50px 0px'
        };

        const observer = new IntersectionObserver((entries) => {
            entries.forEach(entry => {
                if (entry.isIntersecting) {
                    entry.target.style.animation = 'fadeInUp 0.6s ease-out forwards';
                }
            });
        }, observerOptions);

        // Observe all fade-in elements
        document.querySelectorAll('.fade-in').forEach(el => {
            el.style.opacity = '0';
            el.style.transform = 'translateY(30px)';
            observer.observe(el);
        });

        // Counter animation for stats
        function animateCounter(element, target) {
            let current = 0;
            const increment = target / 100;
            const timer = setInterval(() => {
                current += increment;
                if (current >= target) {
                    element.textContent = target.toLocaleString() + (element.textContent.includes('+') ? '+' : '') + (element.textContent.includes('%') ? '%' : '');
                    clearInterval(timer);
                } else {
                    element.textContent = Math.floor(current).toLocaleString();
                }
            }, 20);
        }

        // Animate counters when they come into view
        const statsObserver = new IntersectionObserver((entries) => {
            entries.forEach(entry => {
                if (entry.isIntersecting) {
                    const number = entry.target;
                    const text = number.textContent;
                    const value = parseInt(text.replace(/[^0-9]/g, ''));
                    animateCounter(number, value);
                    statsObserver.unobserve(number);
                }
            });
        });

        document.querySelectorAll('.stat-number').forEach(stat => {
            statsObserver.observe(stat);
        });
    </script>
<script src="js/landing.js"></script>
@inject IJSRuntime JSRuntime

@code {
    protected override async Task OnAfterRenderAsync(bool firstRender)
    {
        if (firstRender)
        {
            await JSRuntime.InvokeVoidAsync("initializeAnimations");
        }
    }
}
